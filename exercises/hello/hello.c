#include "stdio.h" // for html display only, you should use angular brackets
#include "math.h"  // for html display only, you should use angular brackets
int main(void){
	double r=sqrt(2.0);
	printf("hello, world. Did you know that sqrt(2)=%g?\n",r);
return 0;
}
